# ⚡ Быстрый деплой на Netlify

## Вариант 1: Drag & Drop (самый простой)

1. Перейдите на https://app.netlify.com/drop
2. Перетащите эту папку в окно браузера
3. Готово! Сайт будет доступен через несколько секунд

## Вариант 2: Через GitHub

1. Создайте репозиторий на GitHub
2. Загрузите все файлы из этой папки
3. На Netlify нажмите "New site from Git"
4. Подключите GitHub и выберите репозиторий
5. Deploy settings:
   - Build command: (оставьте пустым)
   - Publish directory: (оставьте пустым или укажите `.`)
6. Нажмите "Deploy site"

## Вариант 3: Netlify CLI

```bash
# Установка CLI (один раз)
npm install -g netlify-cli

# Логин (один раз)
netlify login

# Деплой
netlify deploy --prod
```

---

## ⚠️ Важно: добавьте темы!

Архив не содержит готовых тем. Вам нужно:

1. Откройте `HOW_TO_ADD_THEMES.md`
2. Создайте файлы тем в `data/themes/`
3. Добавьте записи в `data/themes.json`
4. Задеплойте изменения

### Минимальный рабочий пример:

**Файл:** `data/themes/example.json`
```json
{
  "id": "example",
  "title": "Пример",
  "description": "Тестовая тема",
  "icon": "🎯",
  "leftColumn": {"title": "A", "type": "a"},
  "rightColumn": {"title": "B", "type": "b"},
  "pairs": [
    {"id": 1, "left": "1", "right": "One", "description": "Один"},
    {"id": 2, "left": "2", "right": "Two", "description": "Два"},
    {"id": 3, "left": "3", "right": "Three", "description": "Три"}
  ]
}
```

**Файл:** `data/themes.json`
```json
[
  {
    "id": "example",
    "title": "Пример",
    "description": "Тестовая тема",
    "icon": "🎯",
    "category": "Тест",
    "author": "Вы"
  }
]
```

---

## 🔧 После деплоя

### Обновление сайта

**GitHub:**
- Просто сделайте push — Netlify автоматически пересоберёт

**Drag & Drop:**
- Перетащите обновлённую папку снова
- Или используйте Netlify CLI: `netlify deploy --prod`

### Настройка домена

1. В настройках Netlify → Domain Settings
2. Add custom domain
3. Следуйте инструкциям

### HTTPS

Netlify автоматически выдаёт SSL-сертификат (бесплатно)

---

## 📊 Проверка работоспособности

После деплоя откройте ваш сайт и проверьте:

- ✅ Главная страница загружается
- ✅ Темы отображаются (если добавили)
- ✅ Можно начать игру
- ✅ Drag & Drop работает
- ✅ Переключение тёмной/светлой темы

Если что-то не работает:
1. Проверьте консоль браузера (F12)
2. Убедитесь, что все файлы загружены
3. Проверьте валидность JSON в темах

---

**Готово! Ваш сайт онлайн! 🚀**
